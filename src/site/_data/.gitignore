# Temporary files generated by builds that point to our CSS/JS entrypoints.
# Eleventy ignores changes in files ignored by a top-level .gitignore, but not by files ignored
# in subdirectories: hence the reason for this file.
resource*.json
