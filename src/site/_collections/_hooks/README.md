# Hooks

The files in this folder export functions that can be used by [11ty's Pagination's `before` callback](https://www.11ty.dev/docs/pagination/#the-before-callback). By hooking into the Pagination lifecycle, we are able to transform the data before it is rendered.
