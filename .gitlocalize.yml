pretranslate:
  segments:
    ignore_patterns:
    - "{% .* %}"
    - "^layout: (.*)"
    - "^(title|authors|date|description): "

  paths:
  - source: "src/site/content/en"
    include:
    - "accessible/**/index.md"